# i18n PT-BR / EN-US Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add EN-US support alongside PT-BR, with a language toggle in the nav that navigates between `/` (PT) and `/en/` (EN) URL trees.

**Architecture:** PT-BR stays at existing routes (zero changes). EN-US pages live under `src/pages/en/` and read from mirrored EN content collections (`blog-en`, `tracks-en`, `track-modules-en`). All UI strings live in a typed `src/i18n/ui.ts` dictionary. `BaseLayout` accepts `lang` and `altHref` props (both optional, default to PT). A `scripts/translate.ts` script generates EN content via Claude Haiku and is run manually — the build requires no Claude API calls.

**Tech Stack:** Astro 5, TypeScript, `@anthropic-ai/sdk`, Vitest, Tailwind CSS.

---

## File Map

| Action | File |
|---|---|
| Create | `src/i18n/ui.ts` |
| Modify | `src/lib/library-schema.ts` |
| Modify | `src/content/config.ts` |
| Create | `scripts/translate.ts` |
| Modify | `package.json` |
| Create | `tests/i18n.test.ts` |
| Modify | `src/layouts/BaseLayout.astro` |
| Modify | `src/pages/index.astro` |
| Modify | `src/pages/sobre/index.astro` |
| Modify | `src/pages/blog/index.astro` |
| Modify | `src/pages/blog/[slug].astro` |
| Modify | `src/pages/trilhas/index.astro` |
| Modify | `src/pages/trilhas/[slug].astro` |
| Modify | `src/pages/library/index.astro` |
| Create | `src/content/blog-en/` (generated by script) |
| Create | `src/content/tracks-en/` (generated by script) |
| Create | `src/content/track-modules-en/` (generated by script) |
| Create | `src/pages/en/index.astro` |
| Create | `src/pages/en/about/index.astro` |
| Create | `src/pages/en/blog/index.astro` |
| Create | `src/pages/en/blog/[slug].astro` |
| Create | `src/pages/en/tracks/index.astro` |
| Create | `src/pages/en/tracks/[slug].astro` |
| Create | `src/pages/en/library/index.astro` |

---

## Task 1: i18n dictionary

**Files:**
- Create: `src/i18n/ui.ts`
- Create: `tests/i18n.test.ts`

- [ ] **Step 1: Create `src/i18n/ui.ts`**

```typescript
export const ui = {
  pt: {
    htmlLang: 'pt-BR',
    nav: {
      brand: 'Vinicius Tirabassi',
      about: 'Sobre',
      blog: 'Blog',
      tracks: 'Trilhas',
      library: 'Biblioteca',
      search: 'Buscar',
    },
    search: {
      placeholder: 'Buscar artigos e recursos...',
      empty: 'Nenhum resultado encontrado.',
      labelBlog: 'Blog',
      labelTrack: 'Trilha',
      labelLibrary: 'Biblioteca',
    },
    home: {
      recentPosts: 'Artigos recentes',
      viewAll: 'Ver todos →',
      noPosts: 'Nenhum artigo publicado ainda.',
    },
    blog: {
      title: 'Blog',
      description: 'Artigos sobre engenharia de software e liderança técnica.',
    },
    tracks: {
      title: 'Trilhas',
      description: 'Cronogramas de estudo estruturados.',
      resources: 'recursos',
      hours: 'horas',
      progress: 'Progresso',
      backToTracks: '← Trilhas',
    },
    library: {
      title: 'Biblioteca',
      description: 'Artigos, vídeos e links que estudo, organizados por tema.',
      addedAt: 'Adicionado em',
      allThemes: 'Todos',
      sidebarTitle: 'Nesta página',
    },
    post: {
      backToHome: '← Voltar para o blog',
    },
    dateLocale: 'pt-BR' as const,
    monthNames: ['Janeiro', 'Fevereiro', 'Março', 'Abril', 'Maio', 'Junho',
      'Julho', 'Agosto', 'Setembro', 'Outubro', 'Novembro', 'Dezembro'] as const,
  },
  en: {
    htmlLang: 'en-US',
    nav: {
      brand: 'Vinicius Tirabassi',
      about: 'About',
      blog: 'Blog',
      tracks: 'Tracks',
      library: 'Library',
      search: 'Search',
    },
    search: {
      placeholder: 'Search articles and resources...',
      empty: 'No results found.',
      labelBlog: 'Blog',
      labelTrack: 'Track',
      labelLibrary: 'Library',
    },
    home: {
      recentPosts: 'Recent articles',
      viewAll: 'See all →',
      noPosts: 'No articles published yet.',
    },
    blog: {
      title: 'Blog',
      description: 'Articles about software engineering and technical leadership.',
    },
    tracks: {
      title: 'Tracks',
      description: 'Structured study schedules.',
      resources: 'resources',
      hours: 'hours',
      progress: 'Progress',
      backToTracks: '← Tracks',
    },
    library: {
      title: 'Library',
      description: 'Articles, videos and links I study, organized by topic.',
      addedAt: 'Added on',
      allThemes: 'All',
      sidebarTitle: 'On this page',
    },
    post: {
      backToHome: '← Back to blog',
    },
    dateLocale: 'en-US' as const,
    monthNames: ['January', 'February', 'March', 'April', 'May', 'June',
      'July', 'August', 'September', 'October', 'November', 'December'] as const,
  },
} as const;

export type Lang = keyof typeof ui;
```

- [ ] **Step 2: Write `tests/i18n.test.ts`**

```typescript
import { describe, it, expect } from 'vitest';
import { ui } from '../src/i18n/ui';

describe('ui dictionary', () => {
  it('pt and en have the same top-level keys', () => {
    expect(Object.keys(ui.pt).sort()).toEqual(Object.keys(ui.en).sort());
  });

  it('pt.nav and en.nav have the same keys', () => {
    expect(Object.keys(ui.pt.nav).sort()).toEqual(Object.keys(ui.en.nav).sort());
  });

  it('pt.search and en.search have the same keys', () => {
    expect(Object.keys(ui.pt.search).sort()).toEqual(Object.keys(ui.en.search).sort());
  });

  it('both have 12 month names', () => {
    expect(ui.pt.monthNames).toHaveLength(12);
    expect(ui.en.monthNames).toHaveLength(12);
  });
});
```

- [ ] **Step 3: Run tests**

```bash
npx vitest run tests/i18n.test.ts
```

Expected: 4 tests pass.

- [ ] **Step 4: Commit**

```bash
git add src/i18n/ui.ts tests/i18n.test.ts
git commit -m "feat(i18n): add ui dictionary with PT and EN strings"
```

---

## Task 2: Update LibraryItem schema

**Files:**
- Modify: `src/lib/library-schema.ts`

- [ ] **Step 1: Add EN fields to `LibraryItem`**

```typescript
export type ResourceType = 'article' | 'video' | 'link';

export interface LibraryItem {
  id: string;
  title: string;
  url: string;
  type: ResourceType;
  theme: string;
  description: string;
  title_en?: string;
  description_en?: string;
  personalNote?: string;
  addedAt: string;
}

export interface LibraryData {
  items: LibraryItem[];
}
```

- [ ] **Step 2: Verify build still passes**

```bash
npm run build 2>&1 | tail -5
```

Expected: `[build] Complete!`

- [ ] **Step 3: Commit**

```bash
git add src/lib/library-schema.ts
git commit -m "feat(i18n): add optional title_en and description_en to LibraryItem"
```

---

## Task 3: Register EN content collections

**Files:**
- Modify: `src/content/config.ts`

- [ ] **Step 1: Add three EN collections with the same schemas as their PT counterparts**

Replace the entire `src/content/config.ts` with:

```typescript
import { defineCollection, z } from 'astro:content';

const blog = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string(),
    date: z.coerce.date(),
    description: z.string(),
    tags: z.array(z.string()).default([]),
  }),
});

const resourceItem = z.object({
  title: z.string(),
  description: z.string(),
  url: z.string(),
});

const tracks = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string(),
    description: z.string(),
    category: z.string(),
    source: z.string(),
    estimatedHours: z.number(),
    domains: z.array(z.object({ name: z.string(), percent: z.number() })).optional(),
  }),
});

const trackModules = defineCollection({
  type: 'content',
  schema: z.object({
    title: z.string(),
    order: z.number(),
    duration: z.string().optional(),
    domain: z.string().optional(),
    officialResources: z.array(resourceItem).optional(),
    complementaryResources: z.array(resourceItem).optional(),
    exercises: z.array(z.string()).optional(),
  }).passthrough(),
});

export const collections = {
  blog,
  tracks,
  'track-modules': trackModules,
  'blog-en': blog,
  'tracks-en': tracks,
  'track-modules-en': trackModules,
};
```

- [ ] **Step 2: Create the EN content directories (so Astro doesn't error if empty)**

```bash
mkdir -p src/content/blog-en src/content/tracks-en src/content/track-modules-en
touch src/content/blog-en/.gitkeep src/content/tracks-en/.gitkeep src/content/track-modules-en/.gitkeep
```

- [ ] **Step 3: Verify build**

```bash
npm run build 2>&1 | tail -5
```

Expected: `[build] Complete!`

- [ ] **Step 4: Commit**

```bash
git add src/content/config.ts src/content/blog-en/.gitkeep src/content/tracks-en/.gitkeep src/content/track-modules-en/.gitkeep
git commit -m "feat(i18n): register blog-en, tracks-en, track-modules-en collections"
```

---

## Task 4: Translation script

**Files:**
- Create: `scripts/translate.ts`
- Modify: `package.json`

- [ ] **Step 1: Create `scripts/translate.ts`**

```typescript
import Anthropic from '@anthropic-ai/sdk';
import fs from 'fs';
import path from 'path';

const client = new Anthropic();

async function translateMarkdown(ptContent: string): Promise<string> {
  const message = await client.messages.create({
    model: 'claude-haiku-4-5-20251001',
    max_tokens: 4096,
    messages: [
      {
        role: 'user',
        content: `Translate the following Markdown file from Portuguese (PT-BR) to English (EN-US).

Rules:
- Translate all human-readable text (titles, descriptions, body text, list items).
- Keep YAML frontmatter keys unchanged; only translate the values.
- Keep technical terms in English (they are already English): e.g. "agentes", "sprint", "ticket", "prompt", "AI", "API", "MCP", brand names, code snippets.
- Keep all URLs unchanged.
- Keep all Markdown formatting (headings, bold, italic, lists, code blocks) unchanged.
- Do NOT add explanations or comments — output ONLY the translated Markdown.

---

${ptContent}`,
      },
    ],
  });

  const block = message.content.find((b) => b.type === 'text');
  if (!block || block.type !== 'text') throw new Error('Empty response from Claude');
  return block.text.trim();
}

async function translateDir(srcDir: string, destDir: string, label: string) {
  if (!fs.existsSync(srcDir)) return;
  fs.mkdirSync(destDir, { recursive: true });

  const entries = fs.readdirSync(srcDir, { withFileTypes: true });

  for (const entry of entries) {
    const srcPath = path.join(srcDir, entry.name);
    const destPath = path.join(destDir, entry.name);

    if (entry.isDirectory()) {
      await translateDir(srcPath, destPath, label);
      continue;
    }

    if (!entry.name.endsWith('.md')) continue;
    if (fs.existsSync(destPath)) {
      console.log(`  skip (exists): ${label}/${entry.name}`);
      continue;
    }

    console.log(`  translating: ${label}/${entry.name}`);
    const content = fs.readFileSync(srcPath, 'utf-8');
    const translated = await translateMarkdown(content);
    fs.writeFileSync(destPath, translated, 'utf-8');
  }
}

async function translateLibrary() {
  const libPath = 'src/data/library.json';
  const data = JSON.parse(fs.readFileSync(libPath, 'utf-8'));
  let changed = false;

  for (const item of data.items) {
    if (!item.title_en) {
      console.log(`  translating library item: ${item.title.slice(0, 50)}`);
      const message = await client.messages.create({
        model: 'claude-haiku-4-5-20251001',
        max_tokens: 200,
        messages: [
          {
            role: 'user',
            content: `Translate this library item from PT-BR to EN-US. Keep technical terms in English. Respond with JSON only, no explanation.\n\n{"title":"${item.title}","description":"${item.description}"}`,
          },
        ],
      });
      const block = message.content.find((b) => b.type === 'text');
      if (!block || block.type !== 'text') continue;
      try {
        const parsed = JSON.parse(block.text.trim());
        item.title_en = parsed.title;
        item.description_en = parsed.description;
        changed = true;
      } catch {
        console.warn(`  failed to parse translation for: ${item.title}`);
      }
    }
  }

  if (changed) {
    fs.writeFileSync(libPath, JSON.stringify(data, null, 2) + '\n', 'utf-8');
    console.log('  library.json updated');
  }
}

async function main() {
  console.log('Translating blog...');
  await translateDir('src/content/blog', 'src/content/blog-en', 'blog');

  console.log('Translating tracks...');
  await translateDir('src/content/tracks', 'src/content/tracks-en', 'tracks');

  console.log('Translating track-modules...');
  await translateDir('src/content/track-modules', 'src/content/track-modules-en', 'track-modules');

  console.log('Translating library items...');
  await translateLibrary();

  console.log('Done.');
}

main().catch((err) => { console.error(err); process.exit(1); });
```

- [ ] **Step 2: Add `translate` script to `package.json`**

In `package.json`, add to `"scripts"`:

```json
"translate": "npx tsx scripts/translate.ts"
```

The scripts block should look like:

```json
"scripts": {
  "dev": "astro dev",
  "build": "astro build",
  "preview": "astro preview",
  "astro": "astro",
  "test": "vitest run",
  "test:watch": "vitest",
  "import:medium": "npx tsx scripts/import-medium.ts",
  "translate": "npx tsx scripts/translate.ts"
},
```

- [ ] **Step 3: Commit**

```bash
git add scripts/translate.ts package.json
git commit -m "feat(i18n): add translate script to generate EN content via Claude Haiku"
```

---

## Task 5: Run the translation script

**Files:** `src/content/blog-en/`, `src/content/tracks-en/`, `src/content/track-modules-en/`, `src/data/library.json`

> Requires `ANTHROPIC_API_KEY` in your environment.

- [ ] **Step 1: Run the script**

```bash
npm run translate
```

Expected output (one line per file + library items):
```
Translating blog...
  translating: blog/artigo-ia.md
  ...
Translating tracks...
  translating: tracks/cca-foundation.md
Translating track-modules...
  translating: track-modules/cca-foundation/...
Translating library items...
  translating library item: O que é o Plugin Superpowers...
  library.json updated
Done.
```

- [ ] **Step 2: Verify EN files were created**

```bash
ls src/content/blog-en/ && ls src/content/tracks-en/ && ls src/content/track-modules-en/
```

Expected: same filenames as the PT directories.

- [ ] **Step 3: Spot-check one translated file**

```bash
head -10 src/content/blog-en/$(ls src/content/blog-en/ | head -1)
```

Verify the frontmatter fields are translated and YAML structure is intact.

- [ ] **Step 4: Commit generated EN content**

```bash
git add src/content/blog-en/ src/content/tracks-en/ src/content/track-modules-en/ src/data/library.json
git commit -m "feat(i18n): add EN translations for blog, tracks and library"
```

---

## Task 6: Update BaseLayout

**Files:**
- Modify: `src/layouts/BaseLayout.astro`

The layout receives two new optional props:
- `lang: Lang` — `'pt'` or `'en'`, defaults to `'pt'`
- `altHref: string` — URL to the same page in the other language, defaults to `'/'`

It uses `ui[lang]` for all nav/search strings and renders a `PT | EN` toggle between the search button and the theme dropdown.

- [ ] **Step 1: Replace the frontmatter section of `BaseLayout.astro`**

Replace lines 1–45 (the `---` block) with:

```astro
---
import { getCollection } from 'astro:content';
import libraryRaw from '../data/library.json';
import type { LibraryData } from '../lib/library-schema';
import { ui } from '../i18n/ui';
import type { Lang } from '../i18n/ui';

interface Props {
  title: string;
  description?: string;
  lang?: Lang;
  altHref?: string;
}
const { title, description = 'Meu site pessoal', lang = 'pt', altHref = lang === 'pt' ? '/en/' : '/' } = Astro.props;
const t = ui[lang];

const library = libraryRaw as LibraryData;
const posts = await getCollection('blog');
const enPosts = await getCollection('blog-en');
const tracks = await getCollection('tracks');

const searchPosts = lang === 'en' ? enPosts : posts;
const searchItems = [
  ...searchPosts.map(p => ({
    type: 'blog',
    title: p.data.title,
    description: p.data.description ?? '',
    url: lang === 'en' ? `/en/blog/${p.slug}` : `/blog/${p.slug}`,
    external: false,
  })),
  ...library.items.map(i => ({
    type: 'library',
    title: (lang === 'en' ? i.title_en ?? i.title : i.title),
    description: (lang === 'en' ? i.description_en ?? i.description : i.description) ?? '',
    url: i.url,
    external: true,
  })),
  ...tracks.map(track => ({
    type: 'trilha',
    title: track.data.title,
    description: track.data.description,
    url: lang === 'en' ? `/en/tracks/${track.slug}` : `/trilhas/${track.slug}`,
    external: false,
  })),
];
---
```

- [ ] **Step 2: Update the `<html>` tag**

Change:
```html
<html lang="pt-BR">
```
To:
```astro
<html lang={t.htmlLang}>
```

- [ ] **Step 3: Replace the nav links block**

Find the nav links section (the four `<a>` tags for Sobre/Blog/Trilhas/Biblioteca) and replace with:

```astro
<a href={lang === 'en' ? '/en/about/' : '/sobre/'}    class="text-apple-nav text-white/80 hover:text-white transition-colors">{t.nav.about}</a>
<a href={lang === 'en' ? '/en/blog/' : '/blog/'}     class="text-apple-nav text-white/80 hover:text-white transition-colors">{t.nav.blog}</a>
<a href={lang === 'en' ? '/en/tracks/' : '/trilhas/'}  class="text-apple-nav text-white/80 hover:text-white transition-colors">{t.nav.tracks}</a>
<a href={lang === 'en' ? '/en/library/' : '/library/'}  class="text-apple-nav text-white/80 hover:text-white transition-colors">{t.nav.library}</a>
```

- [ ] **Step 4: Update the search button label**

Change:
```astro
<span class="hidden sm:inline">Buscar</span>
```
To:
```astro
<span class="hidden sm:inline">{t.nav.search}</span>
```

- [ ] **Step 5: Add the language toggle between the search button and the theme dropdown**

After the search `<button>` closing tag and before the theme `<div class="relative" id="theme-wrapper">`, add:

```astro
<!-- Language toggle -->
<div class="flex items-center gap-1 text-apple-nav shrink-0">
  {lang === 'pt'
    ? <span class="text-white font-semibold">PT</span>
    : <a href={altHref} class="text-white/60 hover:text-white transition-colors">PT</a>
  }
  <span class="text-white/30 select-none">|</span>
  {lang === 'en'
    ? <span class="text-white font-semibold">EN</span>
    : <a href={altHref} class="text-white/60 hover:text-white transition-colors">EN</a>
  }
</div>
```

- [ ] **Step 6: Update the search input placeholder**

Change:
```html
placeholder="Buscar artigos e recursos..."
```
To:
```astro
placeholder={t.search.placeholder}
```

- [ ] **Step 7: Update the search "empty" message in the `<script>` block**

In the `searchInput.addEventListener('input', ...)` handler, replace:

```js
searchResults.innerHTML = '<li class="text-apple-caption text-apple-ink-48 dark:text-[#6e6e73] px-2 py-3">Nenhum resultado encontrado.</li>';
```

With (using `define:vars` to pass the string from Astro):

Add `emptyMessage: t.search.empty` to the existing `define:vars`:

```astro
<script define:vars={{ searchItems, emptyMessage: t.search.empty }}>
```

Then replace the hardcoded string:

```js
searchResults.innerHTML = `<li class="text-apple-caption text-apple-ink-48 dark:text-[#6e6e73] px-2 py-3">${emptyMessage}</li>`;
```

- [ ] **Step 8: Update the search result label logic in the script**

Replace the `label` computation block inside `hits.forEach`:

```js
const label = item.type === 'blog' ? searchLabelBlog
            : item.type === 'trilha' ? searchLabelTrack
            : searchLabelLibrary;
```

And add the three label vars to `define:vars`:

```astro
<script define:vars={{ searchItems, emptyMessage: t.search.empty, searchLabelBlog: t.search.labelBlog, searchLabelTrack: t.search.labelTrack, searchLabelLibrary: t.search.labelLibrary }}>
```

- [ ] **Step 9: Verify build**

```bash
npm run build 2>&1 | tail -5
```

Expected: `[build] Complete!`

- [ ] **Step 10: Commit**

```bash
git add src/layouts/BaseLayout.astro
git commit -m "feat(i18n): update BaseLayout with lang/altHref props and PT|EN toggle"
```

---

## Task 7: Update existing PT pages to pass lang and altHref

**Files:**
- Modify: `src/pages/index.astro`
- Modify: `src/pages/sobre/index.astro`
- Modify: `src/pages/blog/index.astro`
- Modify: `src/pages/blog/[slug].astro`
- Modify: `src/pages/trilhas/index.astro`
- Modify: `src/pages/trilhas/[slug].astro`
- Modify: `src/pages/library/index.astro`

Each page needs two changes:
1. Pass `lang="pt"` to `<BaseLayout>`
2. Pass the correct `altHref` (the EN equivalent URL)

Also use `ui['pt']` strings instead of hardcoded PT text. Import `ui` from `'../../i18n/ui'` (adjust path per file).

- [ ] **Step 1: Update `src/pages/index.astro`**

Add import and update the `BaseLayout` call and hardcoded strings:

```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import { ui } from '../i18n/ui';
const t = ui.pt;

export const prerender = true;

const recentPosts = (await getCollection('blog'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);
---

<BaseLayout
  title="Vinicius Tirabassi"
  description="Artigos sobre engenharia de software e liderança técnica"
  lang="pt"
  altHref="/en/"
>
  <section>
    <div class="flex items-baseline justify-between mb-12">
      <h1 class="text-apple-display font-semibold text-apple-ink dark:text-[#f5f5f7]">{t.home.recentPosts}</h1>
      <a href="/blog" class="text-apple-body text-apple-blue dark:text-apple-blue-dark hover:opacity-70 transition-opacity shrink-0">{t.home.viewAll}</a>
    </div>

    {recentPosts.length === 0 && (
      <p class="text-apple-body text-apple-ink-48 dark:text-[#98989d]">{t.home.noPosts}</p>
    )}
    <!-- rest of template unchanged -->
```

Keep the rest of the template (the `<ul>` and `<li>` blocks) exactly as-is — they use data values, not hardcoded UI strings.

- [ ] **Step 2: Update `src/pages/sobre/index.astro`**

Change the `<BaseLayout>` opening tag only (the bio content stays in Portuguese, the EN version will be a separate page):

```astro
<BaseLayout
  title="Sobre — Vinicius Tirabassi"
  description="Software Engineering Manager @ XP Inc."
  lang="pt"
  altHref="/en/about/"
>
```

- [ ] **Step 3: Update `src/pages/blog/index.astro`**

Add import of `ui`, replace hardcoded month names with `t.monthNames`, update `BaseLayout`:

```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { ui } from '../../i18n/ui';
const t = ui.pt;

export const prerender = true;

function monthId(date: Date): string {
  const name = t.monthNames[date.getUTCMonth()]
    .normalize('NFD').replace(/[̀-ͯ]/g, '').toLowerCase();
  return `${date.getUTCFullYear()}-${name}`;
}

function monthLabel(date: Date): string {
  return `${date.getUTCFullYear()} - ${t.monthNames[date.getUTCMonth()]}`;
}
// ... rest of data logic unchanged
---

<BaseLayout title={t.blog.title} description={t.blog.description} lang="pt" altHref="/en/blog/">
  <!-- template unchanged -->
```

Also update the sidebar "Nesta página" text using `t.library.sidebarTitle` — wait, that's in library. Add a `sidebarTitle` to the blog section of `ui.ts`:

> **Note:** Add `sidebarTitle: 'Nesta página'` to `ui.pt.blog` and `sidebarTitle: 'On this page'` to `ui.en.blog` in `src/i18n/ui.ts` before this step.

- [ ] **Step 4: Update `src/pages/blog/[slug].astro`**

```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { ui } from '../../i18n/ui';
const t = ui.pt;

export const prerender = true;

export async function getStaticPaths() {
  const posts = await getCollection('blog');
  return posts.map((post) => ({ params: { slug: post.slug }, props: { post } }));
}

const { post } = Astro.props;
const { Content } = await post.render();
---

<BaseLayout title={post.data.title} description={post.data.description} lang="pt" altHref={`/en/blog/${post.slug}`}>
  <article>
    <header class="mb-8">
      <p class="text-apple-caption text-apple-ink-48 dark:text-[#98989d] mb-2">
        {post.data.date.toLocaleDateString(t.dateLocale, { day: '2-digit', month: 'long', year: 'numeric' })}
      </p>
      <!-- rest unchanged -->
    </header>
    <div class="prose prose-apple dark:prose-invert max-w-none">
      <Content />
    </div>
    <div class="mt-12 pt-8 border-t border-apple-hairline dark:border-[#3a3a3c]">
      <a href="/" class="text-apple-body text-apple-blue dark:text-apple-blue-dark hover:opacity-70 transition-opacity">
        {t.post.backToHome}
      </a>
    </div>
  </article>
</BaseLayout>
```

- [ ] **Step 5: Update `src/pages/trilhas/index.astro`**

Add to the `<BaseLayout>` tag:

```astro
<BaseLayout title={t_ui.tracks.title} description={t_ui.tracks.description} lang="pt" altHref="/en/tracks/">
```

Add at the top of the frontmatter:

```astro
import { ui } from '../../i18n/ui';
const t_ui = ui.pt;
```

(Use `t_ui` to avoid shadowing the existing `t` variable already used for tracks.)

- [ ] **Step 6: Update `src/pages/trilhas/[slug].astro`**

Add to `<BaseLayout>` tag:

```astro
<BaseLayout title={track.data.title + ' — Trilhas'} description={track.data.description} lang="pt" altHref={`/en/tracks/${slug}`}>
```

Replace the hardcoded "← Trilhas" back-link text:

```astro
import { ui } from '../../i18n/ui';
const t_ui = ui.pt;
// ...
<a href="/trilhas" ...>{t_ui.tracks.backToTracks}</a>
```

- [ ] **Step 7: Update `src/pages/library/index.astro`**

Add import and update `<BaseLayout>`:

```astro
import { ui } from '../../i18n/ui';
const t = ui.pt;
// ...
<BaseLayout title={t.library.title} description={t.library.description} lang="pt" altHref="/en/library/">
```

Replace hardcoded strings:
- `"Todos"` → `{t.library.allThemes}`
- `"Adicionado em"` → `{t.library.addedAt}`
- `"Nesta página"` → `{t.library.sidebarTitle}`
- `"Artigos, vídeos e links que estudo, organizados por tema."` → `{t.library.description}`
- `"Biblioteca"` heading → `{t.library.title}`

- [ ] **Step 8: Verify build**

```bash
npm run build 2>&1 | tail -5
```

Expected: `[build] Complete!`

- [ ] **Step 9: Commit**

```bash
git add src/pages/index.astro src/pages/sobre/index.astro src/pages/blog/index.astro src/pages/blog/\[slug\].astro src/pages/trilhas/index.astro src/pages/trilhas/\[slug\].astro src/pages/library/index.astro
git commit -m "feat(i18n): wire lang/altHref into all PT pages"
```

---

## Task 8: EN home and about pages

**Files:**
- Create: `src/pages/en/index.astro`
- Create: `src/pages/en/about/index.astro`

- [ ] **Step 1: Create `src/pages/en/index.astro`**

```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import { ui } from '../../i18n/ui';
const t = ui.en;

export const prerender = true;

const recentPosts = (await getCollection('blog-en'))
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);
---

<BaseLayout
  title="Vinicius Tirabassi"
  description="Articles about software engineering and technical leadership"
  lang="en"
  altHref="/"
>
  <section>
    <div class="flex items-baseline justify-between mb-12">
      <h1 class="text-apple-display font-semibold text-apple-ink dark:text-[#f5f5f7]">{t.home.recentPosts}</h1>
      <a href="/en/blog/" class="text-apple-body text-apple-blue dark:text-apple-blue-dark hover:opacity-70 transition-opacity shrink-0">{t.home.viewAll}</a>
    </div>

    {recentPosts.length === 0 && (
      <p class="text-apple-body text-apple-ink-48 dark:text-[#98989d]">{t.home.noPosts}</p>
    )}

    <ul class="flex flex-col gap-8">
      {recentPosts.map((post) => (
        <li>
          <a href={`/en/blog/${post.slug}`} class="group block">
            <p class="text-apple-caption text-apple-ink-48 dark:text-[#6e6e73] mb-1">
              {post.data.date.toLocaleDateString(t.dateLocale, { day: '2-digit', month: 'long', year: 'numeric' })}
            </p>
            <h2 class="text-apple-tagline font-semibold text-apple-ink dark:text-[#f5f5f7] group-hover:text-apple-blue dark:group-hover:text-apple-blue-dark transition-colors">
              {post.data.title}
            </h2>
            <p class="text-apple-body text-apple-ink-48 dark:text-[#98989d] mt-1">{post.data.description}</p>
          </a>
        </li>
      ))}
    </ul>
  </section>
</BaseLayout>
```

- [ ] **Step 2: Create `src/pages/en/about/index.astro`**

Translate the bio content. Keep the same HTML structure as `src/pages/sobre/index.astro`, replacing the PT text with EN:

```astro
---
import BaseLayout from '../../../layouts/BaseLayout.astro';
---

<BaseLayout
  title="About — Vinicius Tirabassi"
  description="Software Engineering Manager @ XP Inc. | Leading squads on mission-critical platforms"
  lang="en"
  altHref="/sobre/"
>
  <div class="max-w-2xl mx-auto flex flex-col gap-12">

    <!-- Header -->
    <div class="flex flex-col items-center text-center gap-6">
      <img src="/avatar.jpg" alt="Vinicius Tirabassi" class="w-24 h-24 rounded-full object-cover" />
      <div class="flex flex-col gap-2">
        <h1 class="text-apple-display font-semibold text-apple-ink dark:text-[#f5f5f7]">Vinicius Tirabassi</h1>
        <p class="text-[22px] leading-[1.2] font-normal text-apple-ink-48 dark:text-[#98989d]">
          Software Engineering Manager · XP Inc.
        </p>
      </div>
      <div class="flex gap-3 flex-wrap justify-center">
        <a href="https://github.com/vtirabassi" target="_blank" rel="noopener noreferrer"
           class="bg-apple-blue text-white text-apple-body rounded-full px-[22px] py-[11px] transition-transform active:scale-95">GitHub</a>
        <a href="https://www.linkedin.com/in/viniciustirabassi/" target="_blank" rel="noopener noreferrer"
           class="border border-apple-blue text-apple-blue text-apple-body rounded-full px-[22px] py-[11px] transition-transform active:scale-95">LinkedIn</a>
        <a href="https://medium.com/@viniciustirabassi" target="_blank" rel="noopener noreferrer"
           class="border border-apple-hairline dark:border-[#3a3a3c] text-apple-ink-48 dark:text-[#98989d] text-apple-body rounded-full px-[22px] py-[11px] transition-transform active:scale-95">Medium</a>
      </div>
    </div>

    <!-- Bio -->
    <div class="flex flex-col gap-4 text-apple-body text-apple-ink dark:text-[#f5f5f7]">
      <p>
        I lead engineering squads on mission-critical platforms at XP Inc. —
        <strong>Core Banking</strong> and <strong>Core Investments</strong>, responsible for cash-in and cash-out flows
        across the entire operation. I directly manage 12 engineers across 2 squads.
      </p>
      <p>
        I believe engineering leadership is not about writing less code, but about
        <strong>amplifying the team's impact</strong>. My job is to build environments where engineers make good
        technical decisions, have clarity of purpose, and deliver predictably — even in high-complexity,
        zero-tolerance-for-failure contexts.
      </p>
      <p>
        My background combines a strong technical foundation with management: I started as an engineer building
        XP's Business Digital Account from scratch (PIX, TED, transfers, payments, multi-channel) and now lead
        the teams responsible for that and other core platforms.
      </p>
    </div>

    <!-- What drives my work -->
    <div class="flex flex-col gap-4">
      <h2 class="text-[22px] font-semibold text-apple-ink dark:text-[#f5f5f7] leading-snug">What drives my work</h2>
      <ul class="flex flex-col gap-3 text-apple-body text-apple-ink dark:text-[#f5f5f7]">
        <li class="flex gap-3">
          <span class="text-apple-blue dark:text-apple-blue-dark mt-[2px]">→</span>
          <span>Engineering as a means, business as the end — every technical investment must translate into value for the customer and the operation</span>
        </li>
        <li class="flex gap-3">
          <span class="text-apple-blue dark:text-apple-blue-dark mt-[2px]">→</span>
          <span>Leadership based on trust, autonomy, and context — not control</span>
        </li>
        <li class="flex gap-3">
          <span class="text-apple-blue dark:text-apple-blue-dark mt-[2px]">→</span>
          <span>Feedback culture, high performance, and people development</span>
        </li>
        <li class="flex gap-3">
          <span class="text-apple-blue dark:text-apple-blue-dark mt-[2px]">→</span>
          <span>Architectural decisions connected to strategy — scalability, observability, cost, time-to-market</span>
        </li>
      </ul>
    </div>

    <!-- Stack -->
    <div class="flex flex-col gap-4">
      <h2 class="text-[22px] font-semibold text-apple-ink dark:text-[#f5f5f7] leading-snug">Stack</h2>
      <div class="flex flex-wrap gap-2">
        {['.NET', 'Azure', 'Kafka', 'Microservices', 'Event-Driven', 'Datadog', 'Grafana', 'Dynatrace', 'CI/CD', 'Azure DevOps', 'GitHub Copilot', 'Claude', 'Codex'].map(tag => (
          <span class="text-apple-caption text-apple-ink-80 dark:text-[#98989d] bg-apple-parchment dark:bg-[#2c2c2e] border border-apple-hairline dark:border-[#3a3a3c] rounded-full px-3 py-1">
            {tag}
          </span>
        ))}
      </div>
    </div>

    <!-- Teaching -->
    <div class="border-t border-apple-hairline dark:border-[#3a3a3c] pt-8 text-apple-body text-apple-ink-48 dark:text-[#98989d]">
      For over 3 years I have worked as a <strong class="text-apple-ink dark:text-[#f5f5f7]">University Tutor at FIAP</strong>, in the
      Systems Analysis and Development program. MBA from USP/Esalq.
    </div>

  </div>
</BaseLayout>
```

- [ ] **Step 3: Verify build**

```bash
npm run build 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git add src/pages/en/index.astro src/pages/en/about/index.astro
git commit -m "feat(i18n): add EN home and about pages"
```

---

## Task 9: EN blog pages

**Files:**
- Create: `src/pages/en/blog/index.astro`
- Create: `src/pages/en/blog/[slug].astro`

- [ ] **Step 1: Create `src/pages/en/blog/index.astro`**

Mirror `src/pages/blog/index.astro` but use `blog-en` collection, EN strings, and EN URLs:

```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { ui } from '../../../i18n/ui';
const t = ui.en;

export const prerender = true;

function monthId(date: Date): string {
  const name = t.monthNames[date.getUTCMonth()]
    .normalize('NFD').replace(/[̀-ͯ]/g, '').toLowerCase();
  return `${date.getUTCFullYear()}-${name}`;
}

function monthLabel(date: Date): string {
  return `${date.getUTCFullYear()} - ${t.monthNames[date.getUTCMonth()]}`;
}

const allPosts = (await getCollection('blog-en')).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);

type PostGroup = { id: string; label: string; posts: typeof allPosts };
const groups: PostGroup[] = [];
for (const post of allPosts) {
  const id = monthId(post.data.date);
  const label = monthLabel(post.data.date);
  const g = groups.find(g => g.id === id);
  if (g) g.posts.push(post);
  else groups.push({ id, label, posts: [post] });
}
---

<BaseLayout title={t.blog.title} description={t.blog.description} lang="en" altHref="/blog/">
  <div class="mb-12">
    <h1 class="text-apple-display font-semibold text-apple-ink dark:text-[#f5f5f7]">{t.blog.title}</h1>
    <p class="text-apple-body text-apple-ink-48 dark:text-[#98989d] mt-2">{t.blog.description}</p>
  </div>

  <div class="lg:grid lg:grid-cols-[1fr_200px] lg:gap-16">
    <div>
      {groups.map((group) => (
        <section id={group.id} data-month class="mb-14">
          <h2 class="text-apple-tagline font-semibold text-apple-ink dark:text-[#f5f5f7] mb-6 pb-3 border-b border-apple-hairline dark:border-[#3a3a3c]">
            {group.label}
          </h2>
          <ul class="flex flex-col gap-8">
            {group.posts.map((post) => (
              <li>
                <a href={`/en/blog/${post.slug}`} class="group block">
                  <p class="text-apple-caption text-apple-ink-48 dark:text-[#6e6e73] mb-1">
                    {post.data.date.toLocaleDateString(t.dateLocale, { day: '2-digit', month: 'long', year: 'numeric' })}
                  </p>
                  <h2 class="text-apple-tagline font-semibold text-apple-ink dark:text-[#f5f5f7] group-hover:text-apple-blue dark:group-hover:text-apple-blue-dark transition-colors">
                    {post.data.title}
                  </h2>
                  <p class="text-apple-body text-apple-ink-48 dark:text-[#98989d] mt-1">{post.data.description}</p>
                </a>
              </li>
            ))}
          </ul>
        </section>
      ))}
    </div>

    {groups.length > 0 && (
      <aside class="hidden lg:block">
        <div class="sticky top-20">
          <p class="text-apple-caption font-semibold text-apple-ink-48 dark:text-[#6e6e73] mb-3 uppercase tracking-wide">
            {t.library.sidebarTitle}
          </p>
          <nav>
            <ul class="flex flex-col gap-1">
              {groups.map((group) => (
                <li>
                  <a href={`#${group.id}`} data-month-link={group.id}
                     class="text-apple-caption text-apple-ink-48 dark:text-[#6e6e73] hover:text-apple-blue dark:hover:text-apple-blue-dark transition-colors block py-0.5">
                    {group.label}
                  </a>
                </li>
              ))}
            </ul>
          </nav>
        </div>
      </aside>
    )}
  </div>
</BaseLayout>
```

- [ ] **Step 2: Create `src/pages/en/blog/[slug].astro`**

```astro
---
import { getCollection, getEntry } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { ui } from '../../../i18n/ui';
const t = ui.en;

export const prerender = true;

export async function getStaticPaths() {
  const posts = await getCollection('blog-en');
  return posts.map((post) => ({ params: { slug: post.slug }, props: { post } }));
}

const { slug } = Astro.params;
let post = Astro.props.post;

// Fallback: if no EN version, redirect to PT
if (!post) {
  const ptPost = await getEntry('blog', slug!);
  if (ptPost) return Astro.redirect(`/blog/${slug}`);
  return Astro.redirect('/en/blog/');
}

const { Content } = await post.render();
---

<BaseLayout title={post.data.title} description={post.data.description} lang="en" altHref={`/blog/${post.slug}`}>
  <article>
    <header class="mb-8">
      <p class="text-apple-caption text-apple-ink-48 dark:text-[#98989d] mb-2">
        {post.data.date.toLocaleDateString(t.dateLocale, { day: '2-digit', month: 'long', year: 'numeric' })}
      </p>
      <h1 class="text-apple-display font-semibold text-apple-ink dark:text-[#f5f5f7] leading-tight">{post.data.title}</h1>
      <p class="text-apple-body text-apple-ink-48 dark:text-[#98989d] mt-3">{post.data.description}</p>
      <div class="flex gap-2 mt-4 flex-wrap">
        {post.data.tags.map((tag) => (
          <span class="text-apple-caption bg-apple-parchment dark:bg-[#2c2c2e] text-apple-ink-48 dark:text-[#98989d] px-3 py-1 rounded-full border border-apple-divider dark:border-[#3a3a3c]">{tag}</span>
        ))}
      </div>
    </header>
    <div class="prose prose-apple dark:prose-invert max-w-none">
      <Content />
    </div>
    <div class="mt-12 pt-8 border-t border-apple-hairline dark:border-[#3a3a3c]">
      <a href="/en/" class="text-apple-body text-apple-blue dark:text-apple-blue-dark hover:opacity-70 transition-opacity">
        {t.post.backToHome}
      </a>
    </div>
  </article>
</BaseLayout>
```

- [ ] **Step 3: Verify build**

```bash
npm run build 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git add src/pages/en/blog/
git commit -m "feat(i18n): add EN blog pages"
```

---

## Task 10: EN tracks pages

**Files:**
- Create: `src/pages/en/tracks/index.astro`
- Create: `src/pages/en/tracks/[slug].astro`

- [ ] **Step 1: Create `src/pages/en/tracks/index.astro`**

Mirror `src/pages/trilhas/index.astro` using `tracks-en` collection, EN strings, and `/en/tracks/` URLs:

```astro
---
import { getCollection } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import ProgressBar from '../../../components/ProgressBar.astro';
import { ui } from '../../../i18n/ui';
const t = ui.en;

export const prerender = true;

const tracks = (await getCollection('tracks-en')).sort((a, b) => a.slug.localeCompare(b.slug));

const allModules = await getCollection('track-modules-en');
const resourceCounts: Record<string, number> = {};
for (const mod of allModules) {
  const trackSlug = mod.id.split('/')[0];
  const count = (mod.data.officialResources as any[] | undefined)?.length ?? 0;
  resourceCounts[trackSlug] = (resourceCounts[trackSlug] ?? 0) + count;
}

const categoryColors: Record<string, string> = {
  'certification': 'bg-apple-blue/10 text-apple-blue dark:bg-apple-blue/20 dark:text-apple-blue-dark',
  'certificação':  'bg-apple-blue/10 text-apple-blue dark:bg-apple-blue/20 dark:text-apple-blue-dark',
  'engineering':   'bg-[#34c759]/10 text-[#1a7a33] dark:bg-[#34c759]/20 dark:text-[#34c759]',
  'engenharia':    'bg-[#34c759]/10 text-[#1a7a33] dark:bg-[#34c759]/20 dark:text-[#34c759]',
  'product':       'bg-[#ff9500]/10 text-[#8a5100] dark:bg-[#ff9500]/20 dark:text-[#ff9500]',
  'produto':       'bg-[#ff9500]/10 text-[#8a5100] dark:bg-[#ff9500]/20 dark:text-[#ff9500]',
};

const tracksData = tracks.map(tr => ({
  slug:           tr.slug,
  title:          tr.data.title,
  description:    tr.data.description,
  category:       tr.data.category,
  source:         tr.data.source,
  estimatedHours: tr.data.estimatedHours,
  resourceCount:  resourceCounts[tr.slug] ?? 0,
  href:           `/en/tracks/${tr.slug}`,
  tagClass:       categoryColors[tr.data.category] ?? 'bg-apple-parchment text-apple-ink-48 dark:bg-[#2c2c2e] dark:text-[#98989d]',
}));

const [featured, ...rest] = tracksData;
---

<BaseLayout title={t.tracks.title} description={t.tracks.description} lang="en" altHref="/trilhas/">
  <!-- Same template as trilhas/index.astro, replacing PT href /trilhas/ → /en/tracks/ and using t.tracks.* strings -->
  <!-- Copy the full HTML from src/pages/trilhas/index.astro and update:
       - href="/trilhas" → href="/en/tracks/"
       - "horas" → t.tracks.hours
       - "recursos" → t.tracks.resources
       - Any other hardcoded PT strings → t.tracks.*
  -->
</BaseLayout>
```

> **Important:** Copy the full HTML body from `src/pages/trilhas/index.astro` into this file. Update all hrefs from `/trilhas/` to `/en/tracks/` and replace PT hardcoded strings with `t.tracks.*` equivalents.

- [ ] **Step 2: Create `src/pages/en/tracks/[slug].astro`**

Mirror `src/pages/trilhas/[slug].astro` using `tracks-en` and `track-modules-en`:

```astro
---
import { getCollection, getEntry, render } from 'astro:content';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import ProgressBar from '../../../components/ProgressBar.astro';
import { ui } from '../../../i18n/ui';
const t = ui.en;

export const prerender = true;

export async function getStaticPaths() {
  const tracks = await getCollection('tracks-en');
  return tracks.map(track => ({ params: { slug: track.slug } }));
}

const { slug } = Astro.params;
const track = await getEntry('tracks-en', slug!);
if (!track) return Astro.redirect('/en/tracks/');

const slugPrefix = slug + '/';
const modules = (await getCollection('track-modules-en', ({ id }) => id.startsWith(slugPrefix)))
  .sort((a, b) => a.data.order - b.data.order);

const categoryColors: Record<string, string> = {
  'certification': 'bg-apple-blue/10 text-apple-blue dark:bg-apple-blue/20 dark:text-apple-blue-dark',
  'certificação':  'bg-apple-blue/10 text-apple-blue dark:bg-apple-blue/20 dark:text-apple-blue-dark',
  'engineering':   'bg-[#34c759]/10 text-[#1a7a33] dark:bg-[#34c759]/20 dark:text-[#34c759]',
  'engenharia':    'bg-[#34c759]/10 text-[#1a7a33] dark:bg-[#34c759]/20 dark:text-[#34c759]',
};
const tagClass = categoryColors[track.data.category] ?? 'bg-apple-parchment text-apple-ink-48 dark:bg-[#2c2c2e] dark:text-[#98989d]';

const totalResources = modules.reduce((sum, mod) => {
  return sum + ((mod.data.officialResources as any[] | undefined)?.length ?? 0);
}, 0);

const moduleResourceCounts: Record<string, number> = {};
for (const mod of modules) {
  moduleResourceCounts[String(mod.data.order)] =
    (mod.data.officialResources as any[] | undefined)?.length ?? 0;
}

const renderedModules = await Promise.all(
  modules.map(async (mod) => {
    const { Content } = await render(mod);
    return { mod, Content };
  })
);
---

<BaseLayout
  title={track.data.title + ' — Tracks'}
  description={track.data.description}
  lang="en"
  altHref={`/trilhas/${slug}`}
>
  <!-- Copy full HTML from src/pages/trilhas/[slug].astro and update:
       - href="/trilhas" → href="/en/tracks/"
       - "← Trilhas" → t.tracks.backToTracks
       - "horas" → t.tracks.hours
       - "recursos" → t.tracks.resources
       - "Progresso" → t.tracks.progress
       - date locale → t.dateLocale
  -->
</BaseLayout>
```

- [ ] **Step 3: Verify build**

```bash
npm run build 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git add src/pages/en/tracks/
git commit -m "feat(i18n): add EN tracks pages"
```

---

## Task 11: EN library page

**Files:**
- Create: `src/pages/en/library/index.astro`

- [ ] **Step 1: Create `src/pages/en/library/index.astro`**

Mirror `src/pages/library/index.astro`. Key differences: use `title_en ?? title` and `description_en ?? description`, use `t.library.*` strings, and pass `lang="en"`.

```astro
---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import ShareButton from '../../../components/ShareButton.astro';
import StarRating from '../../../components/StarRating.astro';
import libraryData from '../../../data/library.json';
import { ui } from '../../../i18n/ui';
const t = ui.en;

const { items } = libraryData;
const themes = [...new Set(items.map((item) => (item.title_en ? item.theme : item.theme)))].sort();

const MONTH_NAMES = t.monthNames;

function monthId(dateStr: string): string {
  const [year, month] = dateStr.split('-').map(Number);
  const name = MONTH_NAMES[month - 1]
    .normalize('NFD').replace(/[̀-ͯ]/g, '').toLowerCase();
  return `${year}-${name}`;
}

function monthLabel(dateStr: string): string {
  const [year, month] = dateStr.split('-').map(Number);
  return `${year} - ${MONTH_NAMES[month - 1]}`;
}

type Item = typeof items[0];
type ItemGroup = { id: string; label: string; items: Item[] };

const sortedItems = [...items].sort(
  (a, b) => new Date(b.addedAt).valueOf() - new Date(a.addedAt).valueOf()
);

const groups: ItemGroup[] = [];
for (const item of sortedItems) {
  const id = monthId(item.addedAt);
  const label = monthLabel(item.addedAt);
  const g = groups.find(g => g.id === id);
  if (g) g.items.push(item);
  else groups.push({ id, label, items: [item] });
}

const themeColors: Record<string, string> = {
  'IA Skills': 'bg-apple-blue/10 text-apple-blue dark:bg-apple-blue/20 dark:text-apple-blue-dark',
  'SDD':       'bg-[#34c759]/10 text-[#1a7a33] dark:bg-[#34c759]/20 dark:text-[#34c759]',
  'Harness':   'bg-[#ff9500]/10 text-[#8a5100] dark:bg-[#ff9500]/20 dark:text-[#ff9500]',
};
function themeClass(theme: string): string {
  return themeColors[theme] ?? 'bg-apple-parchment text-apple-ink-48 dark:bg-[#2c2c2e] dark:text-[#98989d]';
}
---

<BaseLayout title={t.library.title} description={t.library.description} lang="en" altHref="/library/">
  <div class="mb-12">
    <h1 class="text-apple-display font-semibold text-apple-ink dark:text-[#f5f5f7]">{t.library.title}</h1>
    <p class="text-apple-body text-apple-ink-48 dark:text-[#98989d] mt-2">{t.library.description}</p>
  </div>

  <div class="flex flex-wrap gap-2 mb-10">
    <button class="filter-btn px-4 py-[11px] text-apple-caption rounded-full border border-apple-blue bg-apple-blue text-white transition-all" data-theme="all">
      {t.library.allThemes}
    </button>
    {themes.map((theme) => (
      <button class="filter-btn px-4 py-[11px] text-apple-caption rounded-full border border-apple-hairline dark:border-[#3a3a3c] text-apple-ink dark:text-[#f5f5f7] hover:border-apple-blue hover:text-apple-blue transition-all" data-theme={theme}>
        {theme}
      </button>
    ))}
  </div>

  <div class="lg:grid lg:grid-cols-[1fr_220px] lg:gap-16">
    <div>
      {groups.map((group) => (
        <section id={group.id} data-month class="mb-14">
          <h2 class="text-apple-tagline font-semibold text-apple-ink dark:text-[#f5f5f7] mb-6 pb-3 border-b border-apple-hairline dark:border-[#3a3a3c]">
            {group.label}
          </h2>
          <ul class="flex flex-col gap-4">
            {group.items.map((item) => (
              <li class="library-item rounded-apple-lg border border-apple-hairline dark:border-[#3a3a3c] dark:bg-[#2c2c2e] p-6" data-theme={item.theme}>
                <div class="flex items-start justify-between gap-6">
                  <div class="flex-1 min-w-0">
                    <div class="flex items-center gap-2 mb-2 flex-wrap">
                      <span class={`text-apple-nav font-medium px-2 py-0.5 rounded-full uppercase tracking-wide ${themeClass(item.theme)}`}>
                        {item.theme}
                      </span>
                    </div>
                    <a href={item.url} target="_blank" rel="noopener noreferrer"
                       class="text-[17px] font-semibold leading-[1.24] tracking-[-0.374px] text-apple-ink dark:text-[#f5f5f7] hover:text-apple-blue dark:hover:text-apple-blue-dark transition-colors block">
                      {item.title_en ?? item.title}
                    </a>
                    <p class="text-apple-body text-apple-ink-48 dark:text-[#98989d] mt-1">{item.description_en ?? item.description}</p>
                    {item.personalNote && (
                      <p class="text-apple-caption text-apple-ink-48 dark:text-[#98989d] mt-3 italic border-l-2 border-apple-hairline dark:border-[#3a3a3c] pl-3">
                        {item.personalNote}
                      </p>
                    )}
                  </div>
                  <div class="flex flex-col items-end gap-3 shrink-0">
                    <ShareButton url={item.url} title={item.title_en ?? item.title} />
                    <StarRating itemId={item.id} />
                  </div>
                </div>
                <p class="text-apple-caption text-apple-ink-48 dark:text-[#6e6e73] mt-4">
                  {t.library.addedAt} {new Date(item.addedAt).toLocaleDateString(t.dateLocale)}
                </p>
              </li>
            ))}
          </ul>
        </section>
      ))}
    </div>

    {groups.length > 0 && (
      <aside class="hidden lg:block">
        <div class="sticky top-20">
          <p class="text-apple-caption font-semibold text-apple-ink-48 dark:text-[#6e6e73] mb-3 uppercase tracking-wide">
            {t.library.sidebarTitle}
          </p>
          <nav>
            <ul class="flex flex-col gap-1">
              {groups.map((group) => (
                <li>
                  <a href={`#${group.id}`} data-month-link={group.id}
                     class="text-apple-caption text-apple-ink-48 dark:text-[#6e6e73] hover:text-apple-blue dark:hover:text-apple-blue-dark transition-colors block py-0.5">
                    {group.label}
                  </a>
                </li>
              ))}
            </ul>
          </nav>
        </div>
      </aside>
    )}
  </div>
</BaseLayout>

<!-- Copy the <script> block verbatim from src/pages/library/index.astro — it is pure DOM JS with no hardcoded strings -->
```

- [ ] **Step 2: Verify build**

```bash
npm run build 2>&1 | tail -5
```

Expected: `[build] Complete!`

- [ ] **Step 3: Commit**

```bash
git add src/pages/en/library/index.astro
git commit -m "feat(i18n): add EN library page"
```

---

## Task 12: Final verification and push

- [ ] **Step 1: Run all tests**

```bash
npm test
```

Expected: all tests pass (including `i18n.test.ts`).

- [ ] **Step 2: Full build**

```bash
npm run build 2>&1 | grep -E "(error|warn|Complete)"
```

Expected: `[build] Complete!` with no errors.

- [ ] **Step 3: Check all EN routes exist in the build output**

```bash
find .vercel/output/static/en -name "index.html" | sort
```

Expected:
```
.vercel/output/static/en/index.html
.vercel/output/static/en/about/index.html
.vercel/output/static/en/blog/index.html
.vercel/output/static/en/blog/[slug]/index.html  (one per article)
.vercel/output/static/en/tracks/index.html
.vercel/output/static/en/tracks/[slug]/index.html
.vercel/output/static/en/library/index.html
```

- [ ] **Step 4: Push**

```bash
git push
```

---

## Acceptance Checklist (from spec)

- [ ] All existing PT routes work without modification
- [ ] All EN routes return 200 with translated content
- [ ] `PT | EN` toggle appears in nav between search and theme buttons
- [ ] `npm run translate` generates EN files without errors
- [ ] Library EN page shows `title_en`/`description_en` when available, falls back to PT
- [ ] Build passes without `ANTHROPIC_API_KEY` (translations are committed files)
